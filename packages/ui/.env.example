# URL del backend a la que apunta el frontend.
# En desarrollo, el proxy de Vite reenvia /api -> ${VITE_API_URL}.
# Copia este archivo a `.env` para desarrollo local.
VITE_API_URL=http://localhost:3000
